import React from "react";
import { Button } from "../Button/Button";

export const Input = () => {
    return (
        <div>
            <input type="text" placeholder="new title" />
           
        </div>
    )
}